import { NostrSigner } from "@jsr/nostrify__types";
import { NUploader } from "@jsr/nostrify__types";
/** NostrBuildUploader options. */ export interface NostrBuildUploaderOpts {
  /** nostr.build endpoint to use. Default: `https://nostr.build/api/v2/upload/files` */ endpoint?: string;
  /** Signer to authenticate with NIP-98 requests. */ signer?: NostrSigner;
  /** Custom fetch implementation. */ fetch?: typeof fetch;
}
/** Upload files to nostr.build or another compatible server. */ export declare class NostrBuildUploader implements NUploader {
  private endpoint: any;
  private signer?: any;
  private fetch: any;
  constructor(opts?: NostrBuildUploaderOpts);
  upload(file: File, opts?: {
    signal?: AbortSignal;
  }): Promise<[['url', string], ...string[][]]>;
  private static schema: any;
}
//# sourceMappingURL=NostrBuildUploader.d.ts.map